# via54Design — Cursor Rules / Windsurf Rules

> 主上下文文件: `AGENTS.md`
> 本文件同时兼容 .cursorrules (Cursor) 和 .windsurfrules (Windsurf) 规范

## 项目身份
**via54Design** — 设计模板引擎 + 叙事引擎 + 媒体管线。CLI 优先, MCP Server 第二。

## 角色定义
- **巫师叔叔 (via54)**: 4A策略总监, 负责设计决策 + YAML模板。不是程序员。
- **AI助手**: 你。写Go代码 + Python管道 + 测试 + 文档。不问审美决策, 直接执行。

## 技术栈
| 层 | 语言 | 外部依赖 |
|----|------|---------|
| 核心引擎 | Go | mcp-go (MIT), yaml.v3 (MIT) |
| LLM管道 | Python 3.11+ | 零 (stdlib only) |
| 设计数据 | YAML | 纯文件 |
| 媒体 | Shell (ffmpeg) | 系统工具 |

## 关键规则 (必须遵守)

### 代码规则
1. Go: 标准库优先, 不引入不必要的第三方包
2. 确定性: 所有 map 遍历用 `sortedKeys[K string, V any]()`
3. 错误链: `fmt.Errorf("xxx: %w", err)` 带上下文
4. 每个 .go 文件: SPDX-License-Identifier 头部
5. PPTX 导出: 只用 `archive/zip` + `encoding/xml`, 不用 unioffice

### 目录规则
- `internal/` + `cmd/`: AGPL-3.0-only
- `templates/` + `hack/` + `docs/`: MIT
- 新平台: 只需加 `templates/prompts/<name>.yaml` → 更新 `prompt_cmd.go` → `pipeline.py`

### 测试规则
- MVP: 3轮 × 4维 (有效性/稳定性/准确性/完整性)
- 边界: 空输入、无效平台、超长字符、特殊字符
- 确定性: 同一输入 → 同一 md5
- 压力: 200次连续生成无错误

### 通信规则
- 决策: 人类决定设计方向, AI执行实现
- 汇报: 先穷尽诊断再报根因, 不接受试错式汇报
- 全修: 发现多个问题时批量全修, 不逐个问

## 当前版本
v0.4.0 | 14平台 × 26维度 | 37/37 Go文件SPDX合规
