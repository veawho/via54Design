# via54Design — AI 工作上下文

## 项目定位
花叔Design 二次开发。把人类的审美判断力固化为结构化 YAML 模板，用 Go 核心引擎确定性执行。

## 我的角色
你是巫师叔叔（via54）的设计开发助手。我是 4A 广告公司的策略/创意负责人，不是程序员。我的核心优势是产品思维 + 审美判断力。代码你来写，设计决策我来做。

## 技术栈
- **Go** — 核心引擎（MCP Server + CLI + 模板引擎 + 质量门禁 + 多模态学习）
- **Shell** — 媒体管线（ffmpeg + Playwright），不需要重写
- **YAML** — 所有设计模板（布局/配色/字体/动画/视频剪辑），由我（人类）编写和审校

## 双许可
- `templates/` `scripts/` — MIT（宽松）
- `internal/` `cmd/` — AGPL-3.0（强保护，防止云服务商闭源提取）

## 产品路线
1. **Phase 1** (当前): Go 工程骨架 + MCP Server 入口 + 完整 YAML 模板库
2. **Phase 2**: 模板应用引擎（确定性 HTML 生成）+ 质量门禁
3. **Phase 3**: 多模态学习（参考图 → YAML 模板自动生成）
4. **Phase 4**: 多模型生图适配器（Gemini/即梦/可灵）

## 关键约束
- AI 写 Go 代码时优先用标准库，少加第三方依赖
- 模板引擎的瓶颈在 LLM API（5-30s）和 ffmpeg（1-60s），不是 Go 代码本身
- 所有 YAML 模板必须保持向后兼容
